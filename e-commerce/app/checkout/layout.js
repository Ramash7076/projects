export const metadata = {
  title: "Checkout - ramash.site",
  viewport:
    "width=device-width, height=device-height, initial-scale=1.0, maximum-scale=1.0",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
